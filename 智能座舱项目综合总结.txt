================================================================================
                智能座舱多模态交互终端 —— 项目综合总结文档
================================================================================

基于以下两个 GitHub 仓库与全部开发对话记录整理而成：
  · https://github.com/1015-jbz/smart_cockpit   （原型项目，代号"小航"，仓库原名 mengchuang）
  · https://github.com/1015-jbz/smart_seat       （演进项目，代号"小龙"，挑战杯主项目）

项目全称：基于 LoongArch 端侧 AI 的智能座舱多模态交互终端设计与实现
作    者：贾秉泽（Jia Bingze）
所属单位：石家庄铁道大学（Shijiazhuang Tiedao University）
参赛背景：挑战杯（全国大学生课外学术科技作品竞赛 / 大学生创业计划竞赛）
目标平台：龙芯 LoongArch 端侧 AI（龙芯 3A6000 笔记本 + UOS 操作系统）

文档整理日期：2026 年 9 月 10 日


================================================================================
第一部分  文档概览与变更日志
================================================================================

## 1.1 文档说明

本文档是对"智能座舱多模态交互终端"项目全过程的一次系统性汇总，覆盖以下
六个维度，力求把"项目从哪里来、要做成什么、做到哪一步、接下来怎么走"讲清楚：

  1. 文档概览与变更日志 —— 本部分，交代文档结构与历次变更脉络；
  2. 产品概述            —— 项目背景、目标、定位、演进关系；
  3. 功能需求与描述      —— 每个功能模块的详细需求说明；
  4. 非功能需求          —— 性能、可靠性、隐私、兼容性、可用性等；
  5. 技术方案与数据需求  —— 系统架构、技术栈、各模块方案、数据流与数据需求；
  6. 发布与运营计划      —— 部署方案、硬件采购、测试运营、风险应对。

文档信息来源于四个渠道，互相印证：
  · smart_cockpit（mengchuang）仓库的 README、总体设计方案、硬件采购清单；
  · smart_seat 仓库的 README、后端/前端代码结构、完整 git 提交历史；
  · 桌面工作记录《智能座舱项目工作记录.txt》（2026-09-08）；
  · 与 AI 助手的多轮开发对话（覆盖疲劳检测、定位、语音播报、硬件选型等）。

## 1.2 变更日志（按时间线汇总）

本节把两个仓库的 git 提交历史与开发对话中产生的重要变更，合并成一条统一的
时间线。带【提交】标记的是已进入代码仓库的 commit，带【对话】标记的是对话
中讨论或临时改动（部分已提交、部分被回退）。

### 阶段 A：原型项目 smart_cockpit（"小航"）

  1. 初始搭建 —— 基于 Python 的 Gradio + Flask 原型，端口 7860/7861，
     集成表情识别（ONNX EfficientNet-B2）、语音对话（DeepSeek）、TTS（edge-tts）、
     安全监控（PERCLOS + 分心检测）。
  2. 修复 20 个已知 bug，核心模块基本可用。
  3. 《01_总体设计方案》成型 —— 规划 7 大软件模块（ASR / NLU / LLM /
     TTS / Safety / Emotion / Control），目标硬件龙芯 3A6000 / 2K2000。
  4. 《硬件采购清单》成型 —— 龙芯 2K0300 开发板方案，预算 550~900 元。
  5. 识别出若干遗留问题：EventBus 存在未确认的 bug；LoongArch
     硬件尚未采购，全部开发在 x86 上完成；对外分享采用 ngrok。

### 阶段 B：smart_seat 项目创立与前端落地

  6. 智能座舱车载助手前端，完整功能实现（React + Vite）。
  7. 完善 README 项目说明和运行指南。
  8. 添加疲劳驾驶警报声（Web Audio 蜂鸣）和摄像头自动问候。
  9. 增强语音助手精确识别与指令执行能力。
  10.天气系统添加自动定位功能。
  11.疲劳数据改为基于真实驾驶时长与摄像头检测指标。
  12.摄像头启动后保持开启 / 随驾驶状态自动开关。
  13.语音助手识别请求后自动关闭麦克风并重新唤醒，导航基于
     当前位置。
  14.smart_seat v1.0 智能座舱系统发布。
  15.安全监控完善：疲劳 / 哈欠 / 分心实时检测 + 前后端联调。
  16.修复 TTS 回环、集成 DeepSeek AI、修复疲劳评分。
  17.优先使用浏览器 GPS 而非 IP 地理定位。

### 阶段 C：龙芯平台适配（先 2K300，后 3A6000）

  18.添加龙芯（Loongson）平台的 setup / start 一键脚本。
  19.全局异常处理器 + 环境变量化 API 地址 + 代码清理。
  20.添加 README（含 Windows 与龙芯双平台安装指南）。
  21.首次适配目标为龙芯 2K0300 先锋派（512MB RAM，LoongArch64）：
      · 尝试安装 onnxruntime / mediapipe LoongArch64 wheels → 成功；
      · 尝试安装 OpenCV LoongArch64 → 成功（需 Haar Cascade 降级方案备用）；
      · 浏览器问题：2K300 无现代浏览器（仅有 dillo 等极简浏览器），
        React 前端无法渲染，JS 执行报 illegal instruction（缺少 LSX SIMD 指令）；
      · 结论：2K300 硬件算力不足（512MB RAM）、缺乏现代浏览器，
        无法作为完整座舱交互终端运行。
  22.调整适配目标为龙芯 3A6000 笔记本（8GB+ RAM，支持完整 LSX/LASX 指令集，
      预装 UOS + 龙芯浏览器 Chromium 138 内核），全栈部署验证通过。
  23.LoongArch 平台适配（onnxruntime / mediapipe 已有预编译
      LoongArch64 wheels，OpenCV 同步适配）。
  24.疲劳检测灵敏度调优。
  23.v2 疲劳算法 + 全局语音架构重构（前端 EWMA + Sustain
     Gate，VehicleStore 统一掌握语音告警节奏）。
  24.TTS 默认音色改为温柔女声（关键词选语音包 + pitch 1.15）。

### 阶段 D：疲劳检测算法 v6 —— 事件驱动告警

  25.疲劳检测 v6：废弃评分驱动的告警触发，改为事件驱动。
     · 哈欠（MAR > 0.6）→ 轻度（warning）告警；
     · 持续闭眼 > 2 秒 → 中度（high）告警；
     · 持续闭眼 > 4 秒 → 重度（critical）告警；
     · 疲劳评分仅作参考展示值，不再参与告警触发。
  26.围绕 v6 的调参：EAR 阈值 0.20、MAR 哈欠阈值 0.6、闭眼 2s/4s 分层、
     告警保持 3s、哈欠冷却 30s、闭眼冷却 15s。

### 阶段 E：高德定位 + 分层评分 + README 同步

  27.高德（Amap）IP 定位 + localStorage 记忆城市，解决定位
     一直显示北京/石家庄的问题。
  28.定位问题排查过程：
     · 起初用高德 JS API key 报 USERKEY_PLAT_NOMATCH（类型不对）；
     · 换成 Web 服务 key 后可用，但返回运营商注册地石家庄，而非
       用户实际所在地邢台；
     · 结论：IP 定位无法到城市级精度，最终用 localStorage 记忆用户手动选择的
       城市（邢台），一次选择、永久生效；
     · 附带排查了"语音播报说北京、界面显示石家庄"的不一致问题。
  29.语音播报城市与界面城市不一致的问题（RightPanel.jsx 问候语 city
     回退值 '北京' 与定位城市不一致）。
  30.疲劳评分改为分层值（闭眼 high=65 / critical=85 / 哈欠
     =30），告警仍保持事件驱动，解决"一闭眼分数就跳到 100"的问题。
  31.同步更新 README（事件驱动疲劳检测 + 高德定位 + 分层
     评分）。
  32.建立工作约定：每次提交 smart_seat 代码时，同步更新 README，保持
     文档与代码一致（已写入持久记忆）。

### 阶段 F：PR 处理与回退

  33.用户处理了一个 PR（声称引入 YOLOv8）。经核查该
     PR 实际并无 YOLO 代码/模型，且把高德定位与 v6 事件驱动疲劳检测一并回退；
     PR 的 start_loongson.sh 还引用了不存在的 app_demo.py。已如实向用户报告。
  34.执行回退：将本地与远端强制回退到高德定位版本，并在此
     基础上重新应用分层评分，告警仍采用事件驱动（不按评分触发播报）。
  35.强制推送（force-with-lease）丢弃合并的 PR。

### 阶段 G：音乐娱乐 / GPS / 多音色 TTS

  36.音乐娱乐模块：本地曲库扫描 + Jamendo 在线曲库 + 语音
     点歌 + 全局播放器。
  37.GPS 定位 + 高德逆地理编码 + 城市搜索 + 防缓存静态服务。
  38.音乐播放体验修复（音量初始化 / 缓冲提示 / 竞态防护）+
     本地演示音轨生成器 + AI 提供商可配置。
  39.合并远端分支 pr4。
  40.8 音色 TTS 服务（角色 / 情绪 / 手动微调三层叠加，音色
     调节真实生效）。
  41.setup 构建前端 dist，start 缺失时自动补构建（修复双击
     后白屏问题）。

### 阶段 H：龙芯 3A6000 笔记本硬件落地

  42.龙芯硬件落地调研与适配（见《工作记录.txt》）：
     · 硬件确定为龙芯 3A6000 笔记本（8GB+ RAM），预装 UOS 操作系统；
     · 3A6000 处理器支持完整的 LSX（128 位 SIMD）和 LASX（256 位 SIMD）
       指令集，可流畅运行现代浏览器及 AI 推理框架；
     · 龙芯浏览器（基于 Chromium 138 内核）原生支持 LoongArch64，
       可直接渲染 React 前端，无需任何降级处理；
     · Python 3.11+ 在 LoongArch64 平台原生可用，setup.sh 脚本自动
       检测并安装合适版本；
     · onnxruntime、mediapipe、opencv-python 均已发布 LoongArch64
       预编译 wheels，无需降级到 Haar Cascade；
     · PyTorch 已适配 LoongArch64，Whisper STT 可在端侧本地运行；
     · 笔记本内置摄像头、麦克风、扬声器均可正常使用，无需外接设备；
     · WiFi 连接稳定，SSH 远程调试可用。
  43.语音识别方案切换：在 Windows 本机部署 openai-whisper 服务
     （whisper_server.py，端口 8787），前端通过 whisperApi.js 调用；
     在龙芯 3A6000 上同样可本地运行 Whisper，实现完全端侧语音识别。
  44.讨论"轻量化部署 AI"的定位：模型选型偏轻量（EfficientNet-B2 ~9M、
     MediaPipe、Haar Cascade），但大模型（DeepSeek）与 ASR 依赖云端/浏览器，
     整体为"边缘端轻量识别 + 云端智能"混合架构；在龙芯 3A6000 上 ASR 已可
     端侧本地运行，进一步向纯端侧轻量化部署迈进。

### 阶段 I：龙芯 3A6000 全面部署与验证

  45.龙芯 3A6000 笔记本全栈部署验证完成：
     · 执行 setup.sh 一键安装脚本，自动完成 Python 3.11+ 环境配置、
       LoongArch64 原生 wheels 安装、前端依赖安装与构建；
     · 执行 start.sh 一键启动脚本，同时拉起 4 个服务进程：
       FastAPI 后端（8000）、Flask 摄像头服务（7861）、TTS 服务（8787）、
       Vite 前端开发服务器（5173）；
     · 在龙芯浏览器中访问 http://localhost:5173，前端页面完整渲染，
       所有模块功能正常；
     · 疲劳检测（MediaPipe 468 关键点 + 事件驱动告警）在 3A6000 上
       实时运行，帧率达到可用水平；
     · 表情识别（ONNX EfficientNet-B2）在 LoongArch64 上直接推理，
       无需降级；
     · 语音助手通过 Whisper 本地 STT + DeepSeek AI 对话 + TTS 合成
       全链路可用；
     · 使用笔记本内置摄像头、麦克风、扬声器，无需外接任何设备。

  46.确认事件驱动语音播报需求：语音播报只应由事件（哈欠/闭眼）触发，
     疲劳评分是"摆设"，不应影响播报。
  47.定位到"分数触发播报"的前端根因：VehicleStore.jsx 在摄像头数据不
     新鲜时会退化为"驾驶时长 + 模拟指标"算分，再反推 alertLevel 触发 startAlertLoop
     → 语音播报。修复方向：把语音循环启停严格 gate 在 camFresh（仅摄像头实时事件
     驱动）上。
  48.定位到"做个表情就提醒疲劳"的后端根因：哈欠判定是单帧 MAR>0.6 即
     触发，张嘴说话/惊讶/大笑都会被误判成哈欠 → warning 播报。修复方向：给哈欠
     加"MAR 持续超阈值 ≥1.5s"的门槛。
  49.最终决定将远端仓库整体回退，本地代码一并覆盖，丢弃此前的临时改动。

> 注：阶段 H 起，smart_seat 的 git 远端地址从 github.com/1015-jbz/smart_seat
  迁移到了 github.com/Au1008333/smart_seat（仓库已迁移/更换账号），代码仓库本体
  仍为同一项目；本文档仍按用户指定的 1015-jbz 地址作为对外引用。

## 1.3 当前代码状态

  · smart_seat 项目仓库：https://github.com/1015-jbz/smart_seat
  · 远端 origin/main：与本地同步；
  · 最近一次操作：将远端回退并强制推送。
  · 龙芯 3A6000 部署状态：已完成全栈部署验证，系统可在 LoongArch64
    平台正常运行。


================================================================================
第二部分  产品概述
================================================================================

## 2.1 项目背景

随着智能网联汽车快速发展，智能座舱已成为汽车"新四化"（电动化、智能化、网联化、
共享化）战略的核心载体。当前主流的智能座舱方案高度依赖云端 AI 服务，普遍存在
三大痛点：

  1. 网络依赖性强 —— 隧道、山区等无网环境功能降级甚至失效；
  2. 隐私安全风险 —— 车内语音/视频数据上传云端，用户隐私难以保障；
  3. 响应延迟不可控 —— 云端往返延迟影响安全预警的实时性。

本项目基于龙芯 LoongArch 架构的端侧 AI 计算能力，构建"全离线、低延迟、高隐私"
的智能座舱多模态交互终端。核心创新点在于将语音交互、视觉感知、大模型推理尽可
能部署在车规级龙芯终端上，实现真正意义上的"端侧智能"。

## 2.2 项目目标

设计文档中设定的量化目标如下：

  | 目标维度         | 指标              |
  |------------------|-------------------|
  | 语音唤醒率       | ≥ 95%（车载噪声） |
  | 语音识别准确率   | ≥ 93%（端侧 ASR） |
  | 疲劳检测准确率   | ≥ 90%（多模态）   |
  | 意图识别准确率   | ≥ 88%（本地 NLU） |
  | 语音响应延迟     | ≤ 500ms（端到端） |
  | 危险预警延迟     | ≤ 200ms           |
  | 大模型推理速度   | ≥ 15 tokens/s     |

（注：以上为目标值；当前原型在龙芯 3A6000 + UOS 上运行，端侧指标已可实测。）

## 2.3 产品定位

  产品定位：一款面向驾驶员的多模态智能座舱交互终端，集"安全守护 + 情感陪伴 +
  语音秘书 + 座舱控制"于一体。

  四大核心角色：
    1. 安全驾驶守护 —— 疲劳/分心实时检测与分级告警；
    2. 情感陪伴助手 —— 表情识别与个性化情感响应；
    3. 智能语音秘书 —— 唤醒词交互、语音点歌、天气/导航查询；
    4. 座舱控制中枢 —— 自然语言车控（空调/车窗/座椅/灯光等）。

## 2.4 两个项目的演进关系

  项目经历了从"原型验证"到"工程落地"的两代演进：

  第一代 smart_cockpit（"小航"，mengchuang）：
    · 形态：Python 单体应用（Gradio + Flask MJPEG），快速验证功能可行性；
    · 特点：本地推理（表情 ONNX、安全 PERCLOS、ASR faster-whisper）+ 云端
      能力（DeepSeek 对话、edge-tts 合成）；
    · 局限：单文件 app_demo.py 体量庞大（6 万字节级），前后端耦合，不利于
      工程化与多人协作。

  第二代 smart_seat（"小龙"）：
    · 形态：前后端分离三层架构（React 前端 + FastAPI 后端 + Flask 摄像头服务）；
    · 特点：模块化、可扩展，新增仪表盘、天气、音乐娱乐、座舱控制等页面；
    · 目标：面向龙芯 3A6000 笔记本的实际部署与挑战杯展示。

  二者共享的技术底座：ONNX EfficientNet-B2 表情模型、PERCLOS/哈欠疲劳检测、
  DeepSeek 对话、TTS 语音合成；smart_seat 进一步引入 MediaPipe FaceLandmarker
  （468 关键点）、SQLite 持久化、WebSocket 车辆数据推送、Whisper ASR 服务。


================================================================================
第三部分  功能需求与描述
================================================================================

## 3.1 功能模块总览

smart_seat 当前功能模块一览：

  | 模块       | 说明                                                      |
  |------------|-----------------------------------------------------------|
  | 仪表盘     | 实时车速/转速/油量/胎压/水温，WebSocket 推送              |
  | 疲劳检测   | MediaPipe 468 关键点 → EAR/MAR → 事件驱动告警             |
  | 表情识别   | ONNX EfficientNet-B2 7 类情绪 + MediaPipe 规则双引擎       |
  | 语音助手   | 唤醒词 + 浏览器/Whisper ASR + DeepSeek AI 对话             |
  | 天气       | Open-Meteo + 高德 IP 定位 + 手动选城（localStorage 记忆） |
  | 安全驾驶   | 分心检测、哈欠计数、视线偏离、疲劳事件记录                |
  | 音乐娱乐   | 本地曲库扫描 + Jamendo 在线曲库 + 语音点歌 + 全局播放器   |
  | 座舱控制   | 自然语言车控（空调/车窗/座椅/灯光/氛围灯）                |

## 3.2 仪表盘模块

  功能需求：
    · 主车速表：0~240 km/h 指针式仪表，含刻度与数字读数；
    · 运行参数：转速 RPM、油量 %、水温 ℃、电量 % 四个迷你仪表；
    · 胎压监测：左前/右前/左后/右后四轮胎压，异常变色告警；
    · 车辆数据：总里程、电池电量、续航里程、平均能耗；
    · 驾驶控制：开始驾驶 / 停车按钮，怠速计时。

  数据来源：WebSocket 推送 + 后端硬件模拟器（hardware_sim.py）。

## 3.3 疲劳检测模块（安全驾驶核心）

  功能需求（当前为事件驱动告警版本 v6）：

    · 眼闭合检测：基于 Eye Aspect Ratio（EAR，MediaPipe 眼周关键点），阈值 0.20；
    · 哈欠检测：基于 Mouth Aspect Ratio（MAR，嘴部关键点），阈值 0.6；
    · 事件 → 告警映射：
        - 哈欠          → warning（轻度）  → 播报"请集中注意力，认真驾驶"；
        - 闭眼 > 2s     → high（中度）     → 播报"您有些疲劳了，请注意休息"；
        - 闭眼 > 4s     → critical（重度） → 播报"危险！请立即停车休息"；
    · 疲劳评分：仅作参考展示值（分层：哈欠=30、闭眼 high=65、critical=85），
      不参与告警触发；
    · 告警冷却：哈欠冷却 30s、闭眼冷却 15s、告警最短保持 3s；
    · 视线方向：三态分类（forward / down / left / right / dashboard / mirror），
      仅展示用。

  关键设计原则（对话中反复确认）：语音播报由"事件"驱动，而非"分数"驱动；
  疲劳评分只是"摆设"，无论分数多高都不应触发播报。

## 3.4 表情识别模块

  功能需求：
    · 7 类情绪分类：angry / disgusted / fearful / happy / neutral / sad / surprised；
    · 双引擎：ONNX EfficientNet-B2 深度学习 + MediaPipe 规则引擎兜底；
    · 悲伤增强后处理（提高 sad 召回）；
    · 情绪统计与历史记录（SQLite 持久化，可查近 N 天分布）；
    · 情绪驱动的座舱联动建议（氛围灯、音乐音量、座椅温度等）。

## 3.5 语音助手模块

  功能需求：
    · 全局唤醒词："小龙"（smart_cockpit 为"你好小航"）；
    · 语音/文字双模态输入；
    · 对话能力：DeepSeek 大模型多轮记忆 + 情绪感知，离线模板兜底；
    · 语音合成：多音色 TTS（温柔女声默认、8 音色、角色/情绪/手动微调三层叠加）；
    · 语音点歌、天气查询、导航等指令执行。

  数据链路：麦克风 → ASR（浏览器 SpeechRecognition 或本地 Whisper 服务 8787）→
  意图/对话（DeepSeek）→ TTS（浏览器 speechSynthesis 或 tts_server.py）。

  龙芯 3A6000 部署说明：在 LoongArch64 平台上，Whisper STT 通过 PyTorch
  LoongArch64 原生版本本地运行，无需依赖浏览器 Web Speech API，可实现完全
  离线语音识别。

## 3.6 天气模块

  功能需求：
    · 实时天气 + 未来预报；
    · 定位策略（优先级）：高德 IP 定位 → pconline → ipinfo（后端多级 fallback）；
    · 手动选城 + localStorage 记忆（解决 IP 定位到不了城市级的问题）；
    · 支持全国 30+ 城市坐标库。

## 3.7 安全驾驶模块（页面）

  功能需求：
    · 疲劳评分仪表盘 + 分级告警展示；
    · 蜂鸣警报（Web Audio，非语音，区别于语音播报）；
    · 疲劳事件记录与上报（POST /safety/fatigue/event）。

## 3.8 音乐娱乐模块

  功能需求：
    · 本地曲库扫描（music_library.py）+ Jamendo 在线曲库（music_online.py）；
    · 语音点歌；
    · 全局播放器（MusicStore 状态管理 + MiniPlayer 组件）；
    · 本地演示音轨生成器（generate_demo_tracks.py）。

## 3.9 座舱控制模块

  功能需求：
    · 自然语言车控：空调（温度/风量）、车窗（升降）、座椅（加热）、灯光（氛围灯）；
    · 场景模式与联动规则（雨天关窗、疲劳提醒等，设计阶段）。


================================================================================
第四部分  非功能需求
================================================================================

## 4.1 性能需求

  | 指标             | 要求                       |
  |------------------|----------------------------|
  | 疲劳告警延迟     | 事件检测到播报 < 1s（轮询 1.2s） |
  | 表情识别实时性   | 摄像头流式，CPU 实时推理  |
  | 语音响应延迟     | 端到端 ≤ 500ms（目标值）  |
  | 前端更新周期     | 安全数据 1.2s / 摄像头 2s 轮询 |
  | 大模型推理       | ≥ 15 tokens/s（量化后目标）|

## 4.2 可靠性与健壮性

  · 后端全局异常处理器；
  · 摄像头/模型不可用时的降级（MediaPipe → Haar Cascade）；
  · 定位多级 fallback（高德 → pconline → ipinfo）；
  · 天气多级 fallback（后端 → 前端直连 Open-Meteo）；
  · 疲劳事件记录静默降级（后端不可用仅 console.warn）；
  · 音乐播放竞态防护、缓冲提示。

## 4.3 隐私与安全

  · 设计目标：本地推理优先，减少车内音视频上云；
  · DeepSeek API Key 走 .env 环境变量，不入库；
  · 已知风险：早期曾把 DeepSeek key 明文写入 .claude/settings.json，需迁移到
    环境变量/密钥管理。

## 4.4 兼容性（龙芯平台适配）

  · LoongArch 平台（龙芯 3A6000）：onnxruntime / mediapipe / opencv 均有
    LoongArch64 预编译 wheels，无需降级，全功能可用；
  · 浏览器兼容：龙芯浏览器（基于 Chromium 138 内核）原生支持 LoongArch64，
    React 前端可完整渲染，无任何兼容性问题；
  · Python 环境：Python 3.11+ 在 LoongArch64 平台原生可用，setup.sh 脚本
    自动检测并安装合适版本；
  · Python 依赖：优先使用龙芯官方源 pypi.loongnix.cn/loongson/pypi，
    PyPI 官方源已有 LoongArch64 wheels 的包可直接安装。

## 4.5 可用性

  · 一键安装/启动脚本（setup.bat / start.bat / setup.sh / start.sh）；
  · 温柔女声默认音色、多音色可调；
  · 全局录音指示条、唤醒词聆听状态提示。


================================================================================
第五部分  技术方案与数据需求
================================================================================

## 5.1 系统架构

smart_seat 采用前后端分离三层架构：

  浏览器（React 19 + Vite，端口 5173）
    ├── :8000/api/v1/* ──→ FastAPI 后端（端口 8000）──→ SQLite
    │                         ├── DeepSeek API（AI 对话）
    │                         ├── Open-Meteo / 高德（天气与定位）
    │                         └── 音乐库（本地 + Jamendo）
    ├── :7861/video_feed ─→ Flask 摄像头服务（端口 7861）
    │       /api/state          ├── MediaPipe 468 关键点
    │                           ├── ONNX EfficientNet-B2
    │                           └── Haar Cascade（fallback）
    ├── :8787 ─────────→ Whisper ASR 服务（本机，语音转文字）
    └── ws://:8000/ws/vehicle ─→ WebSocket 车辆数据推送

  龙芯 3A6000 部署架构：
    使用笔记本内置摄像头、麦克风、扬声器，start.sh 一键启动全部 4 个服务，
    龙芯浏览器访问 http://localhost:5173 即可使用完整系统。

## 5.2 技术栈

  | 层级     | 技术                                  |
  |----------|---------------------------------------|
  | 前端     | React 19 + Vite + TailwindCSS 4       |
  | 后端     | FastAPI + SQLAlchemy + SQLite         |
  | 摄像头   | Flask + OpenCV + MediaPipe + ONNX     |
  | 语音     | 浏览器 SpeechRecognition / Whisper + speechSynthesis / tts_server |
  | 大模型   | DeepSeek API                          |
  | 视觉模型 | MediaPipe FaceLandmarker、ONNX EfficientNet-B2（enet_b2_7.onnx）|

  LoongArch64 平台技术栈补充：
  | Python   | 3.11+（LoongArch64 原生）            |
  | 浏览器   | 龙芯浏览器（Chromium 138 内核）       |
  | AI 框架  | onnxruntime-loongarch64、mediapipe-loongarch64 |
  | 深度学习 | PyTorch-loongarch64（Whisper STT）    |
  | 操作系统 | UOS（统一操作系统）                   |

## 5.3 后端代码结构

  backend/
    main.py                 # FastAPI 入口
    config.py               # 配置中心（含高德 AMAP_KEY）
    camera_server.py        # Flask 摄像头 + 表情/疲劳检测
    database.py / models.py / schemas.py  # SQLite ORM 与 Pydantic
    tts_server.py           # 多音色 TTS 服务
    static_server.py        # 防缓存静态服务器
    pregen_voices.py        # 预生成音色
    generate_demo_tracks.py # 本地演示音轨生成
    routers/                # chat / driving / emotion / location / music /
                            # safety / vehicle / weather
    services/               # chat_agent / emotion_engine / fatigue /
                            # hardware_sim / location_proxy / music_library /
                            # music_online / weather_proxy

## 5.4 前端代码结构

  src/
    pages/       # VehicleDashboard / DrivingSafety / EmotionRecognition /
                 # VoiceAssistant / Weather / MusicPlayer / CabinControl /
                 # Dashboard / NotFound
    context/     # VehicleStore（全局车辆+安全状态）/ VoiceStore（语音队列）/
                 # MusicStore（音乐播放）/ ThemeContext（主题）
    components/  # CameraFeed / RightPanel / NotifyPanel / MiniChat /
                 # MiniPlayer / GaugeChart / RecordingBar / Sidebar / Layout ...
    services/    # api.js（后端客户端）/ weatherApi.js（天气直连）

## 5.5 疲劳检测技术方案（演进历程）

  检测输入：MediaPipe FaceLandmarker 提取 468 个人脸关键点。

  · EAR（眼部长宽比）计算：取左眼(33,133,159,145)与右眼(362,263,387,373)的
    上下/左右距离比，二者取均值；阈值 0.20 判定闭眼。
  · MAR（嘴部长宽比）计算：上唇(13)/下唇(14)距离 ÷ 嘴角(61)/嘴角(291)距离；
    阈值 0.6 判定哈欠。
  · PERCLOS：眼睑闭合时间占比（设计阶段用于疲劳评分参考）。

  算法演进：
    v1/v2   —— 评分驱动：多特征加权（PERCLOS 70% + 哈欠 30% 等）算疲劳分，
               再由分数映射告警等级，前端 EWMA + Sustain Gate 平滑；
    v6      —— 事件驱动：废弃评分管线，直接由事件（哈欠/闭眼）决定告警等级，
               评分仅作参考展示；
    v6+分层 —— 评分改为分层值（闭眼 high=65/critical=85/哈欠=30），避免"一闭眼
               就涨到 100"。

  遗留讨论（对话中已定位、最终回退未合并）：
    · 前端在摄像头数据不新鲜时会退化为"驾驶时长 + 模拟指标"算分并反推告警，
      导致"分数触发播报"——正确做法是语音循环严格 gate 在 camFresh 上；
    · 后端哈欠单帧触发，导致"张嘴表情误判成哈欠"——正确做法是加 MAR 持续
      ≥1.5s 的门槛。

## 5.6 定位技术方案

  三级 fallback（location_proxy.py）：
    1. 高德 IP 定位（restapi.amap.com/v3/ip，解析 rectangle 取中心点）；
    2. pconline IP 定位；
    3. ipinfo.io。

  已知局限：IP 定位只能到运营商注册地城市，无法精确到用户实际城市（邢台）；
  解决方案：前端 localStorage 记忆手动选择的城市，一次选择永久生效。

## 5.7 数据需求

  数据库（SQLite）：
    · 车辆数据（车速/转速/胎压等，WebSocket 推送 + 模拟器）；
    · 疲劳事件记录（POST /safety/fatigue/event：score、level、duration、action）；
    · 情绪记录与统计（POST /emotion/records、GET /emotion/stats）。

  模型文件（不入库）：
    · models/enet_b2_7.onnx（表情，~30MB，GitHub Release 自动下载）；
    · models/face_landmarker.task（MediaPipe 人脸关键点）；
    · faster-whisper tiny（ASR，~75MB，HuggingFace 自动下载）。

  外部 API 依赖：
    · DeepSeek（AI 对话）；Open-Meteo（天气）；高德（IP 定位）；Jamendo（在线音乐）。

  环境变量：
    · 前端 .env.local：VITE_API_BASE / VITE_CAMERA_BASE / VITE_APP_TITLE /
      VITE_DEFAULT_USERNAME；
    · 后端 .env：DEEPSEEK_API_KEY / HOST / PORT / AMAP_KEY。


================================================================================
第六部分  发布与运营计划
================================================================================

## 6.1 部署方案

  开发阶段（Windows x86）：
    · 三个终端分别启动 FastAPI（8000）、摄像头（7861）、前端（5173）；
    · 语音识别本机 Whisper 服务（8787）。

  龙芯 3A6000 笔记本（LoongArch64 + UOS）：
    · 一键安装 setup.sh（自动完成 Python 3.11+ 环境配置、LoongArch64 原生
      wheels 安装、前端依赖安装与构建）；
    · 一键启动 start.sh（同时启动 4 个服务：FastAPI 后端、Flask 摄像头、
      TTS 服务、Vite 前端）；
    · 龙芯浏览器（基于 Chromium 138 内核）访问 http://localhost:5173
      即可使用完整系统；
    · 使用笔记本内置摄像头、麦克风、扬声器，无需外接设备。

  对外分享：
    · 局域网直连（同一 WiFi 访问本机 IP）；
    · 公网访问采用 ngrok 隧道。

## 6.2 硬件采购计划

  依据《硬件采购清单》（2026-07-27），方案为"龙芯 2K0300 端侧 + PC/车载算力盒
  端边协同"，总预算约 550~900 元：

  | 类别       | 关键件                        | 预算     |
  |------------|-------------------------------|----------|
  | 核心平台   | 龙芯 2K0300 开发板（512MB）   | ~330 元  |
  | 视觉       | 罗技 C270 USB 摄像头           | ~120 元  |
  | 音频       | 绿联 USB 麦克风 + USB 小音箱   | ~80 元   |
  | 效果加分   | WS2812B 灯带 +杜邦线          | ~20 元   |
  | 显示（选） | 7 寸 HDMI 电容触摸屏           | ~180 元  |
  | 网络（选） | USB WiFi 网卡（RTL8188）       | ~20 元   |

  适配结果：
    · 2K0300 开发板（512MB）：已采购并完成初步适配，但因硬件限制放弃；
    · 实际最终部署硬件：龙芯 3A6000 笔记本（8GB+ RAM），预装 UOS 操作系统；
    · 笔记本内置摄像头、麦克风、扬声器、WiFi，无需额外购买外设。

## 6.3 硬件落地进展与关键约束

  龙芯 2K0300 先锋派适配（已完成，结论：不适用）：
    · 已采购龙芯 2K0300 开发板（512MB RAM，LoongArch64）；
    · onnxruntime / mediapipe LoongArch64 wheels 可安装运行；
    · 但 512MB 内存不足以支撑多服务并行（后端 + 摄像头 + TTS + 前端）；
    · 无现代浏览器：仅有 dillo 等极简浏览器，无法渲染 React 前端；
    · JS 执行报 illegal instruction：部分指令缺少 LSX SIMD 支持；
    · 结论：2K0300 适合作为轻量 IoT 网关，不适合作为智能座舱交互终端。

  龙芯 3A6000 笔记本适配（已完成，结论：全栈可用）：
    · UOS 操作系统安装并正常运行；
    · Python 3.11+ 环境配置完成，setup.sh 自动安装 LoongArch64 原生 wheels；
    · onnxruntime、mediapipe、opencv-python 均已安装 LoongArch64 预编译包；
    · PyTorch LoongArch64 版本安装完成，Whisper STT 可本地运行；
    · 龙芯浏览器（Chromium 138 内核）安装完成，可正常渲染 React 前端；
    · 4 个服务进程通过 start.sh 一键启动，运行稳定；
    · 疲劳检测、表情识别、语音助手、音乐娱乐、天气查询等全部功能验证通过；
    · 使用笔记本内置摄像头、麦克风、扬声器，全功能可用。

  龙芯 3A6000 平台技术优势：
    · 处理器支持 LSX（128 位 SIMD）和 LASX（256 位 SIMD）指令集，
      可流畅运行现代浏览器及 AI 推理框架；
    · 8GB+ 内存满足前端构建、后端服务、AI 模型加载等多进程并行需求；
    · UOS 系统生态完善，常用开发工具及 Python 包均有 LoongArch64 适配。

## 6.4 测试与验收计划

    · 语法检查：py_compile 校验 Python 后端；
    · 功能测试：对着摄像头打哈欠 → warning 播报；闭眼 2s → high；闭眼 4s →
      critical 蜂鸣；
    · 定位测试：高德 IP 定位 + 手动选城持久化；
    · 语音测试：唤醒词 → ASR → DeepSeek 对话 → TTS 播报；
    · 龙芯验收：系统启动、Python 环境、摄像头、浏览器渲染（已全部通过）；
    · 龙芯 3A6000 全栈测试：4 服务一键启动、全部功能模块正常运行。

## 6.5 风险与应对

  | 风险                     | 应对                                       |
  |--------------------------|--------------------------------------------|
  | 端侧算力不足             | INT4 量化 + 模型级联（轻量初筛→大模型精判）|
  | 2K300 无现代浏览器       | 已解决：适配目标切换为 3A6000 + 龙芯浏览器 |
  | 2K300 缺少 LSX SIMD 指令 | 已解决：3A6000 支持完整 LSX/LASX 指令集    |
  | 2K300 512MB 内存不足     | 已解决：3A6000 配备 8GB+ RAM               |
  | LoongArch 无预编译包     | 已解决：onnxruntime/mediapipe/opencv/PyTorch
                             均有 LoongArch64 原生 wheels |
  | IP 定位不准              | 手动选城 + localStorage 记忆               |
  | 车载噪声干扰 ASR         | 麦克风阵列 + 频域降噪 + 车载声学模型微调   |
  | 情绪识别主观性           | 多模态融合 + 用户反馈闭环                  |

## 6.6 后续路线图

  Phase 1（已完成）：Windows 原型开发，功能闭环；
  Phase 2（已完成）：功能联调与优化，音乐/多音色 TTS/GPS 已并入；
  Phase 3（已完成）：LoongArch 移植（2K300 适配后切换至 3A6000，全栈部署验证通过）；
  Phase 4（进行中）：端侧性能调优、答辩演示与实车/座舱模型集成。


================================================================================
第七部分  龙芯 3A6000 部署详情
================================================================================

## 7.1 部署环境概述

  龙芯 3A6000 笔记本是本项目的最终目标部署平台，主要配置如下：

  | 项目       | 配置                                      |
  |------------|-------------------------------------------|
  | 处理器     | 龙芯 3A6000（LoongArch64 架构）           |
  | 内存       | 8GB+ DDR4                                 |
  | 操作系统   | UOS（统一操作系统）                        |
  | 浏览器     | 龙芯浏览器（Chromium 138 内核，原生 LoongArch64） |
  | 摄像头     | 内置摄像头（免驱）                          |
  | 麦克风     | 内置麦克风（免驱）                          |
  | 扬声器     | 内置扬声器                                  |
  | 网络       | 内置 WiFi                                   |

## 7.2 安装流程

  步骤 1：克隆代码
    git clone https://github.com/1015-jbz/smart_seat.git
    cd smart_seat

  步骤 2：一键安装（自动适配 LoongArch64）
    chmod +x setup.sh
    ./setup.sh

  setup.sh 脚本自动完成以下操作：
    · 检测系统架构，确认为 LoongArch64；
    · 安装或检测 Python 3.10+ 环境（优先使用系统自带 Python 3.11+）；
    · 创建 Python 虚拟环境并安装依赖；
    · 安装 LoongArch64 原生 wheels（onnxruntime、mediapipe、opencv-python、
      PyTorch 等）；
    · 安装 Node.js 依赖并构建前端 dist；
    · 生成 backend/.env 配置模板。

  步骤 3：配置 API Key
    vim backend/.env
    # 填入 DEEPSEEK_API_KEY（从 platform.deepseek.com 获取）

  步骤 4：一键启动
    chmod +x start.sh
    ./start.sh

  start.sh 脚本同时启动 4 个服务：
    · FastAPI 后端（端口 8000）
    · Flask 摄像头服务（端口 7861）
    · TTS 语音合成服务（端口 8787）
    · Vite 前端开发服务器（端口 5173）

## 7.3 使用龙芯浏览器访问

  启动服务后，打开龙芯浏览器（基于 Chromium 138 内核），访问：
    http://localhost:5173

  龙芯浏览器原生支持 LoongArch64，可完整渲染 React 前端，所有功能模块
  正常可用，无需任何降级或特殊处理。

  注意事项：
    · 首次打开需授权摄像头和麦克风权限；
    · 龙芯浏览器支持 Web Speech API，可使用浏览器内置语音识别；
    · 如需更高精度的本地语音识别，可使用 Whisper STT 服务（端口 8787）。

## 7.4 内置设备使用说明

  龙芯 3A6000 笔记本内置设备均可直接使用：

  摄像头：
    · 用于疲劳检测和表情识别；
    · 系统启动后自动调用，无需手动配置；
    · 支持 MediaPipe 468 关键点检测。

  麦克风：
    · 用于语音助手唤醒和语音识别；
    · 支持浏览器 Web Speech API 和 Whisper STT 两种识别方式；
    · 系统自动检测麦克风设备。

  扬声器：
    · 用于语音播报和音乐播放；
    · 支持 TTS 多音色语音合成；
    · 音量可通过座舱控制面板调节。


================================================================================
附：对话中讨论过的关键技术问答要点
================================================================================

  1. 轻量化部署 AI 定位：模型选型偏轻量（EfficientNet-B2、MediaPipe、Haar），
     但大模型/ASR 依赖云端，属"边缘轻量识别 + 云端智能"混合架构，非纯端侧；
     在龙芯 3A6000 上 ASR 已可端侧本地运行，进一步向纯端侧部署迈进。
  2. OLED / LCD 兼容性：龙芯板卡以 HDMI/USB 输出为主，7 寸 HDMI 电容触摸屏
     （免驱）较稳妥，OLED 无现成接口；
  3. 传感器选型：推荐 USB 免驱设备（麦克风/音箱/摄像头），避免 3.5mm 驱动问题；
     温度/湿度、车辆对接优先考虑 OBD-II（ELM327）或 CAN 总线，龙芯经 USB/串口
     适配；龙芯 3A6000 笔记本内置设备均可免驱使用。
  4. 语音点歌实现：ASR 识别"播放 XX 的歌" → 意图解析 → 本地曲库/在线曲库检索 →
     全局播放器播放；
  5. 免驱温度检测模块：优先 I2C 传感器（如 DHT/DS18B20）+ 电平转换，或 USB 串口
     温度计；
  6. 高德地图差异化小功能：逆地理编码、城市搜索、IP 定位中心点解析等。

================================================================================
结语
================================================================================

本文档汇总了"智能座舱多模态交互终端"从原型（smart_cockpit/"小航"）到工程化
（smart_seat/"小龙"）两代演进过程中的全部关键信息：项目设计初衷、量化目标、
功能与非功能需求、技术方案、数据需求、发布运营计划，以及历次对话中产生的算法
迭代（疲劳检测事件驱动化、高德定位、语音播报驱动方式等）与硬件落地进展。

核心结论：项目在软件功能上已基本闭环（多模态交互 + 安全检测 + 娱乐 + 车控），
算法层面已明确"事件驱动告警、评分仅参考"的正确设计；龙芯 3A6000 笔记本全栈
部署已验证通过，系统在 LoongArch64 + UOS 平台上完整运行，龙芯浏览器可正常
渲染前端，onnxruntime/mediapipe/opencv/PyTorch 均有 LoongArch64 原生支持，
Whisper STT 可在端侧本地运行，使用笔记本内置摄像头、麦克风、扬声器即可完成
全部功能演示。项目已具备答辩演示与实车集成的条件。

================================================================================
（完）
================================================================================
